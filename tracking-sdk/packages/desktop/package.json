{
  "name": "@stillkinetic/desktop-sdk",
  "version": "0.1.55",
  "description": "StillKinetic tracking and Stripe billing-consent UI for Electron, Tauri, and npm-compatible desktop WebViews.",
  "license": "MIT",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts --out-dir dist --clean",
    "dev": "tsup src/index.ts --format esm,cjs --dts --out-dir dist --watch",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@stillkinetic/web-sdk": "^0.1.5",
    "@stripe/stripe-js": "^3.0.0"
  },
  "devDependencies": {
    "@stillkinetic/web-sdk": "^0.1.5",
    "@stripe/stripe-js": "^3.5.0",
    "tsup": "^8.0.2",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=18"
  },
  "publishConfig": {
    "access": "public"
  }
}
